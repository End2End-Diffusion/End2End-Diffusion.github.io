# DiffusionBench Project Page

Project page for **DiffusionBench: On Holistic Evaluation of Diffusion Transformers**.

Scaffolded from the `repa-e-t2i` project page template.

## View locally

```bash
cd nanogen
./serve.sh
# open http://localhost:8008
```

## Structure

```
nanogen/
├── index.html          # Main page (skeleton — fill in sections)
├── references.bib      # Citations; reference via <d-cite key="...">
├── serve.sh            # Local preview server
└── static/
    ├── css/            # style.css, custom.css, fontawesome
    ├── js/             # distill template, zoom, tabs, nav-bar, ...
    ├── img/icons/      # section + key-point icons
    └── webfonts/       # fontawesome fonts
```

Shared site-wide styles live in `../static/css/paper-layout.css` and the
favicon in `../static/img/kitty.png`.

## TODO

- [ ] Confirm author list / affiliations (currently "Authors TBD")
- [ ] Add paper (arXiv) link once available
- [ ] Fill in section content + figures under `static/img/`
- [ ] Finalize BibTeX entry
